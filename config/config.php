<?php
/**
 * WordPress configuration constants.
 *
 * Define site-level WordPress constants here. Use if (!defined()) before each define() —
 * platform-managed constants take precedence, and the guard avoids PHP notices on redefinition.
 *
 * For a full reference of available constants, see:
 * https://developer.wordpress.org/advanced-administration/wordpress/wp-config/
 *
 * Do not store sensitive values (credentials, API keys) here.
 */

/**
 * Example: define a custom constant using a guard.
 *
 * Replace WPE_EXAMPLE_CONSTANT with the constant you want to define.
 */
/*
if ( ! defined( 'WPE_EXAMPLE_CONSTANT' ) ) {
    define( 'WPE_EXAMPLE_CONSTANT', true );
}
*/
